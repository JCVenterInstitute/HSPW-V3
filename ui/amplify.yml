version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - if [ "$ENV" = "prod" ]; then npm run build:prod; else npm run build:dev; fi
    artifacts:
      baseDirectory: build
      files:
        - "**/*"
