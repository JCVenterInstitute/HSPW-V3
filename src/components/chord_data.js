/*
const [countSaliva, setCountSaliva] = useState("");
const fetchSalivaCount = async () => {
  try {
    const res = await fetch("http://localhost:8000/countProteinS")
      .then((res) => res.json())
      .then((data) => setCountSaliva(data.datarows[0]));
  } catch (error) {
    console.error("Error fetching data:", error);
  }
};

*/
